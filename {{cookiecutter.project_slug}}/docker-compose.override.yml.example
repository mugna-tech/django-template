version: '3.6'

services:
    {{ cookiecutter.project_slug }}_web:
        command: bash -c "python manage.py runserver 0.0.0.0:8000"
        environment:
            DEBUG: 'True' # This will be converted into a boolean value.